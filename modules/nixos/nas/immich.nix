{ ... }:

{
  # Immich — photo management (self-hosted Google Photos alternative)
  # Uncomment once /mnt/ssd2/Photos exists and you're ready to set it up

  # services.immich = {
  #   enable       = true;
  #   openFirewall = true; # port 2283
  #   mediaLocation = "/mnt/ssd2/Photos";
  # };
}
